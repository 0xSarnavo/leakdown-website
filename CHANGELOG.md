# Changelog

Every user-visible change gets a line under **Unreleased** in the same commit.
Cutting a release moves that block under a version heading and bumps `package.json`.

## Unreleased

- Pin `engines.node` to `24.x`, the newest Vercel offers for builds and functions (26 exists only in their Sandbox product). The old `>=20` let a host pick up a new Node major on its own, and both Vercel and Nixpacks read this field, so the two deploys now agree.
- The hero no longer fades in on load. The headline lines and the drop paint at full opacity on the first frame, which is also what a link preview and a skimming visitor see. Removes the `rise-in` keyframe, which was defined twice in the stylesheet.
- Runs on Vercel as well as Railway, with no per-host code. The CSP nonce uses `btoa` instead of `Buffer`, so middleware is fine on the Edge runtime; API routes pin `runtime = "nodejs"` for the SigV4 signer; the client IP prefers Vercel's signed `x-vercel-forwarded-for` and still takes the first entry otherwise. The per-IP rate limit moves to Upstash Redis over REST when `UPSTASH_REDIS_REST_URL` and `_TOKEN` are set (no SDK, one fetch), since a module-level Map only holds on one long-lived box; it fails open and logs.
- Repo cleanup: dropped the dead `how-section.tsx` component, `hero-graphic.svg`, and the two hero JPEGs the CSS stopped painting (moved to `../local/hero-artwork/`, 556K out of the deploy). The hero background rules no longer carry `background-image: none`.
- Live readout: between 720px and 900px the log used to sit on top of the leak tags. The stacked narrow layout now applies under 900px.
- README lists the `plan`, `brief` and `publish` fields on `POST /request` and the `POST /api/early-access` route. `*.tsbuildinfo` is ignored.
- Docs moved to their own site, docs.leakdown.ai (the new leakdown-docs app). Nav 05 Docs, the footer's Docs and Changelog links point there and open in a new tab, and /docs on this site redirects to it.
- Subtle motion across the site (sections no longer fade in as they scroll): the hero headline and drop settle in on load, buttons lift on hover and give on press, the live terminal and video lift a touch on hover, FAQ questions and footer links nudge on hover, and the nav underline slides in. All of it stops under reduced motion. The page scrollbar is hidden (scrolling still works).
- Every Request a run on the site (hero, nav button, features walkthrough, request section, footer, sample report, 404) now opens the request popup instead of scrolling; without JS the links still go to the request section. The hero command is restyled: a terminal field with a purple edge that slowly travels round it, a purple prompt, the repo path dimmed, a blinking cursor and a divided copy button, beside a separate purple Request a run button (in place of Run now) with a glow and a light sweep on hover; on phones the button drops under the field. The popups gain the mark beside their title, a lit top edge, labels that turn purple while you type, a light sweep on the button, and early access ends on a check that draws itself. Get early access opens its own popup that asks only for an email, stored through a new additive `POST /api/early-access` (same gate, body cap and rate limit as `/request`; existing routes unchanged).
- The hero drop now turns slowly on its axis: the light, a sweeping highlight and faint longitude bands travel across its dots. Clicking the nav logo plays a different effect each time, in turn: the rows burst and spring back, the mark spins a full turn, the rows collapse and refill top down, or a purple ripple rings out as the dots flash. All still under reduced motion.
- Nav follows the page: 01 Live / 02 Features / 03 FAQ / 04 Request / 05 Docs (the CLI README, new tab). The link for the section on screen lights up purple, the bar's button reads Request a run, and under 900px a menu button opens a panel with the same links (Esc closes it). The footer gains a FAQ link.
- Privacy page now lists everything a requested run keeps: the run choice and brief, the optional public-sharing consent, and the run's videos, logs and screenshots; findings go public only on opt-in and once fixed, and deletion covers the recordings.
- Alpha note between the live run and the features walkthrough: one large statement (Leakdown is in alpha; a stalled prospect is a signal, not a measurement; under three sessions it says too few to call; check it on your own page) whose words light up in reading order as you scroll past, under an edge-to-edge rule. No pinning, so the page barely grows. Reduced motion shows it fully lit.
- FAQ section above the request call to action: a slim "Frequently asked questions" strip like the live section's head, over seven full-width questions: what a requested run (the free demo) collects and keeps, then six that each name the CLI, answered from its README in a one-open accordion (answers slide open in large, bright type, the question turns purple on hover, chevron flips).
- Request call to action: **Run it yourself, or have us run it.** at hero-headline size, no sub line, held on screen under the nav for a couple of scrolls, edge to edge with no frame rules. While it holds, scrolling makes it rain purple drops (harder the faster you scroll), the tide of purple dots rises with the scroll, and the surface sloshes with scroll speed before settling; scroll back and it drains. A light drizzle keeps going at rest and the surface swells under the cursor. Request a run is the main button and opens the request form in a quiet, single-column modal box (title, underline inputs, a Full run / Special run toggle with a one-line summary of what a full run includes or a box for what to test, small consent checkboxes, one purple button with "The report lands in your inbox." under it; Esc, the close button or a click outside closes it); Get early access (email) sits beside it and a small link points to the open-source CLI. The box defaults to a full run and lists what it includes; picking Special run asks what to test. A second, optional checkbox lets people allow us to share real findings publicly (posts, case studies, ads) once fixed. The request API takes these as additive optional fields (`plan`, `brief`, `publish`) and stores them on the order; existing fields, strings, caps and routes are unchanged. The hero's Run now, nav 03 Request and every Request a run link work again. Reduced motion shows it unpinned, half full and still.
- Links to other sites (GitHub, X, LinkedIn) now open in a new tab.
- Live section: the "Catch them before you lose them." strip is slimmer (same type size), and the rule under it now runs edge to edge.
- Hero drop: the headline now sits behind it; its drip gathers, necks and lets go with no splash, fading out before it reaches the sub line, with exactly one falling drop per drip (it used to fire several in a row) and its travel scaled to the room above the sub line; rim dots now grow and shrink with the wobble instead of blinking; and the cursor parts its dots like a finger through water. The drop stays in place on scroll (no fall or splash). A gap between "new" and "web" lets the drop's tip show through; the headline stays centred and the drop and its callouts shift to sit under the gap. The sub line and command box sit closer under the drop, and the headline is larger (up to 124px, from 104px). The captcha callout sits higher and the quote callout sits closer to the drop. The logo drop is wider, closer to the hero's proportions.
- One mark everywhere: the halftone dot teardrop from the hero is now the logo in the nav and footer, the favicon, the footer's isometric block and the report plate. It animates as a whole (a wave through the dots and a liquid squash in the logo, a wobble, bob and ripple in the hero) while purple water gathers at its base, necks down and falls away (no splash). The favicon animates too: a script redraws the tab icon from a canvas about ten times a second, with the static SVG as the fallback.
- Hero rebuilt on a centred editorial layout: headline, a large teardrop drawn as a halftone dot field (lit from the top left, slowly rippling, leaking purple droplets from a crack near its base) overlapping it, thin dot-and-hairline callouts for a sample run, then the sub line and the command box with Run now. The animated slash band is gone from the hero; the 404 page keeps its field.
- Hero layout back to centred copy on a plain background with the animated field in a ruled band along the bottom (inspired by a banking reference). The band keeps the cursor-following glow and leak words, and adds slowly drifting, airy patches of $ and £: the money leaking out.
- Hero is now field-first: the slash field fills the whole hero behind the copy, a soft dark pool keeps the text readable, and on desktop the purple glow follows the cursor and decrypts leak words where it settles (it roams on its own otherwise). Logo motion: a light scans down the bars and the missing block drips out with a squash; hovering glitches the bars and clicking the nav logo bursts and springs them back. The report plate in the features walkthrough now carries the mark, a 3/10 headline, three findings and a fix-first strip. Favicon drops its background and follows the tab's light or dark scheme.
- New Leakdown mark: a pixel droplet drawn as scanlines with one block missing from its belly that falls out below it on a loop (still under reduced motion). It replaces the dashed logo placeholder in the nav and footer, the round drop on the footer's isometric block, and serves as the favicon.
- Accent is now deep purple site-wide instead of blue: #5b21b6 on the cream theme, #9d7bf5 on the dark theme (the deeper #7c3aed only reaches 3.3:1 on dark, too low for link text), with the terminal, 404 page and glows following. The footer keeps the deeper #7c3aed in dark mode for its blocks.
- Footer rebuilt: brand block with tagline and GitHub / X / LinkedIn / email icons, Product / Open source / Legal link columns (plain links that turn blue on hover), "© 2026 Leakdown. All rights reserved." under matching outline icons, and a giant Leakdown wordmark beside an isometric pair of blocks whose top block (the leak mark) lifts off and docks onto the base on a loop, with a glow when they meet. The footer uses a deep purple accent (#7c3aed dark, #5b21b6 light) instead of the site blue. Reduced motion shows them docked.
- Remove the request section (two doors, form, closer) from the homepage while the lower sections are rebuilt one by one; the form component and its API stay.
- Remove the "How it works" section (the live terminal and the features walkthrough already tell that story), its footer link, and its styles. Nav is now `01 Live / 02 Features / 03 Request / 04 Docs`.
- Features: the six-card grid is replaced by a pinned walkthrough under the heading "Find the leak before it costs you a customer." Four steps advance on a rail as you scroll (Watch, Unblock, Guard, Fix), each naming the pain first, while an isometric stack of four plates on the right lifts the matching plate and shows its callouts. The last step carries a Request a run button, and a bottom strip tracks the step. Phones and reduced motion get a plain stacked list. The walkthrough and its separators run edge to edge, like the run readout above it.
- Live terminal is now a zsh-style replay of real `leakdown` output: typed prompt, stage banners, crawl and thinking spinners, per-step `[n/12 · x/10 confusion]` lines, ABANDONED/COMPLETED blocks, an agents-finished bar that redraws in place, the ladder's filter/verify/report lines, then `clear` and `--history` and a `--goal` pass run. Placeholder site and people only.
- Live section: the four fact cards are replaced by a pinned, scroll-driven run readout. As you scroll, a run log prints and one bar per sample-report leak rises out of a slash field with its label (the single-session agent finding is orange and marked as needing a second run), over a SAMPLE RUN strip with 10 prospects sent and 3 leaks cited by 2+. Reduced motion shows the finished state without pinning. The readout runs full-bleed to the viewport edges; on wide screens the slash field slides left as you scroll so each bar rises near the right edge and travels with it, and the log prints a matching LEAK line as each bar rises (tail of six lines). SAMPLE RUN links to the sample report. The terminal and video now fill their cells. The page frame widens from 1400px to 1800px so side margins shrink, and the nav links sit beside the logo at every width instead of centring on wide screens. The hero artwork is off the page for now; both image files stay in `public/assets/`.
- Drop the numbered eyebrow labels above every section head (and on the legal and sample pages). Cells get roomier side padding on desktop. Hero scrims melt into the page over more stops, and theme switches cross-fade instead of snapping. Live terminal now types the CLI's real commands (`--ladder`, `--goal`, `--flow`, `--doctor`, `--history`) and prints the stage lines the tool logs; the session clip plays at 2.5x with no pause control. 404 background is the hero's slash field with its glow circling the text and 404-themed words.
- 404 page rebuilt as a fixed full-screen scene: drifting glyph field seen through a roaming round spotlight (same glide-and-rest path as the hero glow, blue bloom inside, black vignette outside), giant dashed numerals, a blue pill and a glass pill, and the small sitemap / sample report / request links.
- Hero: the glow now roams the slash field on an eased path biased toward the centre, parks for about a second, and decrypts two or three words inside the lit area each stop (the words are now the failures a run surfaces: STUCK, FORM RESET, AGENT BLOCKED, CITED 3/10, SEALED); the headline, subhead, and command bar sit lower; the light-mode scrim is thinner so the desk artwork shows through.
- Harden the request API and headers: the body cap now aborts the stream at 4096 chars instead of buffering first, bearer compare works on byte length (a multibyte token no longer 500s), CSP adds `object-src 'none'`, HSTS gains `includeSubDomains`, plus `x-frame-options: DENY` and `cross-origin-opener-policy: same-origin`; the form's fetches send no credentials and never cache. Rename `middleware.ts` to `proxy.ts` (Next 16 convention) and stop Next dev from writing `AGENTS.md`/`CLAUDE.md`. Accessibility: skip link, a pause control on the looping clip, named regions on the animated terminal and install block, footer links at a 24px target.
- Redesign the homepage onto one pattern: the hero and live-run grid set the language, and every later section now sits inside the same 1400px ruled frame as the nav (section head strip + hairline cell grid). Cut the free-run band, sample teaser, why, FAQ, and final-CTA sections (the risk line and safety notes moved under the request doors; the sample report stays at `/sample-report`). Sections no longer stretch to a full viewport, so the page is roughly a third of its old height. Nav: `01 Live / 02 Features / 03 How / 04 Request / 05 Docs`. Reveal motion shortened and staggered per cell; video plays only while on screen; hero canvas skips work offscreen.
- Convert hero artwork from PNG to JPEG (2.9M → 556K), add `stale-while-revalidate` caching for `/assets/*`, and pause the hero canvas loop when scrolled offscreen.
- Remove the retired static stack (`server.mjs`, `public/*.html|css|js|robots.txt|sitemap.xml`) now that the Next.js app serves everything; the operator endpoints live only under `/api/*` via the legacy rewrites. Consolidate the route 500 handler into `lib/orders.ts` and dedupe the CLI repo URL into `lib/site.ts`.
- Port the early-access intake (`POST /request`) and operator queue (`/orders`) from `server.mjs` into Next.js API routes (`/api/*`, legacy paths via rewrites) with identical statuses, error strings, caps, rate limits, and bearer auth.
- Rebuild the homepage on the locked D1 skeleton (install-first hero, live terminal + session video, badged features, scroll-driven how-it-works, request-a-run doors) with self-hosted fonts, real OG card, and the legacy request form behavior.

## 1.1.0 — 2026-09-15

**Canonical domain is leakdown.ai.** Canonical link, social cards, sitemap, and
robots all point at `https://leakdown.ai` (was the Railway preview URL).
